# Local Cloudflare / Wrangler state
.wrangler/
.dev.vars
.env
.env.*

# Dependency / tooling caches
node_modules/.cache/
node_modules/.mf/

# Ebook binaries (upload to R2 separately)
*.pdf

# OS / editor
.DS_Store
*.log
